# CHANGELOG

## v1.0 — 2026-09-19

首个完整版本。从零设计 UI 并落地为可运行的本地应用。

### 设计阶段
- 建立视觉方向：暖调家常感。奶油底 `#F8F5F0` + 番茄橙主色 `#E8703A` + 鼠尾草绿辅色 `#87A878`。
- 定下核心视觉纪律：**主色每屏只出现一次**，只给当前屏唯一的主动作按钮。
- 产出 6 屏设计稿 + 1 页设计规范板：
  首页（今天吃什么）· 随机结果 · 菜品详情 · 烹饪步骤 · 菜谱库 · 我的口味
- 12 道菜的成品图与过程图由 AI 生成，挑选并压缩后入库 10 张。

### 开发阶段
- 将设计稿 1:1 落地为单文件 `app/index.html`（1274 行），严格还原布局、配色、圆角、间距。
- 固定 390×844 画布 + 等比缩放，窄屏自动切真机全屏模式。
- 实现加权随机抽菜、忌口硬过滤、动态「选择理由」文案。
- 实现份数换算（`BASE_SERVE = 2` 为基准，支持 1–6 人）。
- 用 localStorage 做持久化（key `chishenme.v1`）：忌口、历史、口味偏好、本周目标。
- 图片压缩：原图 1536×1024 / 约 2 MB × 10 张 → 长边 ~880 / JPEG q84，合计约 0.9 MB。

### 相对设计稿的三处有意偏离
1. **筛选标签统一**：首页与菜谱库原先标签文案不一致，统一为
   `全部 / 20分钟快手 / 下饭硬菜 / 清淡少油`，避免同一概念两个说法。
2. **「晚餐提醒」改为「本周目标」**：原稿里这是一个纯装饰的假开关，
   改为可实际记录、能算出进度的真实功能。
3. **新增「记录」页**：设计稿 6 屏中没有抽菜历史，实际使用时很需要，因此新增第 7 屏。

### 修复记录（开发中踩到并解决的坑）
- **图标被裁切**：初版 SVG 构造器重复附加 `viewBox` 导致图形溢出被剪。
  统一为 `svgV(vb,w,h,inner)` / `ic(size,inner)`，所有图标画在 24×24 坐标系后缩放。
- **整屏空白**：`render()` 未给根节点加 `is-active` 类，`.screen` 默认 `display:none`。已修。
- **移动端内容溢出**：首页「最近吃过」竖列表与详情页步骤列表超出 844px。
  首页改为横向滚动条、菜品图高度 188→180；详情页拆分为「食材」与「做法」两屏，做法页加进度条。
- **详情页入栈逻辑错误**：从详情页再开详情会反复入栈。改为「已在详情页则原地重绘」。
- **`localStorage` 脏数据**：外部写入或旧版本残留会导致渲染报错。
  新增 `sanitize()`，对历史记录、忌口数组逐项做了类型与有效性校验。
- **XSS 风险**：用户输入的忌口词直接拼进 HTML。新增 `esc()` 转义。
- **清理死代码**：删除未使用的 CSS 类；把硬编码的 `#C4BEB3` 换成令牌 `C.chev`。

### 验证
- 两轮 Node 静态自检（类名 / 图标名 / 状态字段 / 资源引用交叉核对），修复后 0 个真实问题。
- 确认 10 个图片引用与 10 个磁盘文件完全对应，无失效引用。

---

## 下一轮候选（按优先级）

1. **扩菜谱到 30–50 道** —— 现在只有 8 道，抽几次就重复了，这是最影响体验的短板。
   加菜只需在 `DISHES` 数组追加一项 + 放一张图。
2. **按冰箱食材反查** —— 输入现有食材，推荐能做出来的菜。目前已有食材数据结构，可直接比对。
3. **一键采购清单** —— 选中多道菜后合并食材生成清单，去重、按份数累加。
4. **拆成 Vite + React 工程** —— 若要长期维护。`DISHES` 抽成 JSON，7 屏拆成路由。
5. **打包成 APK / PWA** —— 纯前端形态很适合直接封装上手机。
6. **周计划** —— 排一周菜单并导出，配合候选 3 的采购清单形成闭环。
