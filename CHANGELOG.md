# CHANGELOG

## v3.0.0 — 2026-09-19

菜谱库从 150 道扩到 **500 道**，并做到 **500 道全部配图**（不再有占位块）；
同时按反馈重做了详情页份量、做菜流程、头像三处交互。

### 新增
- **菜谱扩容 150 → 500 道**：家常菜 60→200 / 汤羹 20→70 / 主食 25→80 /
  凉菜 15→50 / 早餐 15→50 / 夜宵 15→50。新增 350 道全部含结构化食材用量 + 4~6 步做法。
- **配图补全 65 → 500 张**：新生成 435 张成品图（沿用 v2.0 实拍风格），
  统一压缩为 WebP（长边 800 / 30~50KB），**图片覆盖率 100%**。
- **详情页可手动改份量**（需求②）：食材配料区加 `− N 人份 +` 步进器（1~8 人份），
  食材用量实时等比换算；份量**按道记忆**（退出重进仍保留），持久化到 IndexedDB prefs。
  调整时只重绘摘要行 / 份量控件 / 食材区，不打断滚动位置。
- **「开始做菜」一页到底**（需求③）：去掉「上一步 / 下一步」分页，
  进入即整页纵向铺开全部步骤与细节；新增同页备料清单、
  随滚动推进的粘性进度条（第 N 步 / 共 M 步），底部一键「做完收工 · 记入记录」。
- **厨师头像**（需求④）：「我的」页「小厨日记」与首页右上角由通用用户图标
  换成手绘厨师形象（内联 SVG，48×48 坐标系，自带暖橙圆底，深浅色 / 多倍屏均清晰）。
- **菜谱库分批渲染**：500 道规模下首批渲染 60 道 + 「加载更多」按钮，
  避免一次性铺 500 个卡片拖慢首屏；切分类 / 搜索自动回到第一批。
- 结果页「份量」改为显示该道菜自己的份量，与详情页联动。
- **支持装到手机主屏（PWA）**：新增 `src/manifest.webmanifest` 与 `src/sw.js`
  —— 核心资源预缓存 + **500 张菜谱图后台分批预热** + cache-first 回源；
  菜谱图清单从 `data/manifest.js` 推导，新增菜谱不用改 SW。
  iPhone 用 Safari 打开 <https://tianyoufeng.github.io/zuocai-app/> →「分享」→
  「添加到主屏幕」，全屏无地址栏，缓存完即可离线；Android Chrome 同样支持。
  APK 内的 WebView 壳（`app.local`）不注册 SW，两条路互不影响。
- 新增 `tools/make_pwa_icons.py`（从 icon-480 生成 180/192/512）、
  `_dev/pwa-check.js`（PWA 离线自检 12 项）、
  `.github/workflows/pages.yml`（push 到 main 自动把 `src/` 发布到 GitHub Pages）。

### 变更
- `tools/compress_images.py`：入库时裁掉图片底部 10% —— ImageGen 出图右下角带平台
  「AI 生成」水印，裁掉后画面干净、构图不受影响。
- `tools/build-apk.sh`：中间目录改由 `tools/clean_work.py` 清理
  （shell 的 `rm -rf` 会触发批量删除确认，构建会被打断）。
- 首页 hero 文案「共 N 道家常菜」→「共 N 道菜谱」（已含 6 大分类）。

### 修复
- **`compress_images.py` 分类判定误用子串匹配**：`jiangrou-bao` 命中了
  `jiangrou-baozi`，导致「酱肉包 / 酱香饼」两张图被归到 `zhushi/` 而非 `zaocan/`
  → 改为 id 字面量精确匹配（`id:\s*'xxx'`），并把两张错位图复位。
- **端到端自检存在假通过**：详情页「食材清单已懒加载」等的是 `ing-row` 数量 > 0，
  而加载态占位行本身就是 1 行，该项恒真 → 改为等真实数据（>1 行且不含「加载中」）。
  修好后立刻测出并修正了「滚动到底进度条仍显示第 1 步」的真实 bug。
- **份量换算漏掉括号里的数字**：`2 个（约 400 g）` 在 4 人份下变成
  `4 个（约 400 g）` —— 重量没跟着涨。原因是 `RANDOM.scaledAmount` 只换算用量
  **开头**的数字 → 改为换算用量里的**所有**数字（`适量 / 少许 / 半只` 仍然不动）。
  这个 bug 是人工看关键屏截图时发现的，已补进端到端自检用例防回归。
- `_dev/static-check.js` 同步补上 `data-dserve / data-finish / data-more` 三个
  事件分支与 `serveOf / recipeIngs / libShown` 三个状态字段的核对项。

### 交付与校验
- APK：`dist/chishenme-v3.0.0.apk`（**24 MB**，versionCode 30，零权限，v2/v3 签名有效）
  → 安装包改由 **GitHub Releases** 分发，**不再入版本库**：
  每轮迭代都要推 24MB 二进制，仓库会迅速膨胀；本地 `dist/` 仍保留产物。
- 数据校验：500 道 · 有图 500 道 · 0 错误
- 静态一致性：6/6 · 端到端浏览器自检：**38/38**（真实控制台错误 0）
- PWA 离线自检：**12/12**（线上实测：SW 激活后 25 秒内预热完 500 张图，
  断网重载仍载入 500 道菜谱、首页正常渲染、图片命中缓存）
- APK 逐字节核对通过 · 包内菜谱图片 500 张
- 线上（PWA）：<https://tianyoufeng.github.io/zuocai-app/> —— push 到 main 自动部署

### 已知限制
- APK 体积从 3.2MB 增至 24MB —— 完全离线 + 500 张内置图，这是必然代价。
  若要压体积，可把菜品图降到长边 640 / 质量 60（预计省 40%），代价是清晰度下降。
- iOS 仅交付文档（Windows 无法编译 iOS 工程）。

---

## v2.0.0 — 2026-09-19

从 v1 单文件 demo 到完整可安装 App 的大版本。设计稿 7 屏 1:1 落地，
150 道菜谱 + 65 张真实成品图，零权限 Android APK。

### 新增
- **菜谱库扩容 10→150 道**：家常菜 60 / 汤羹 20 / 主食 25 / 凉菜 15 / 早餐 15 / 夜宵 15，
  每道含食材用量 + 分步做法 + 时间/难度/热量/标签。
- **分类浏览**：菜谱库分类条（全部 + 6 大分类），首页保留「快手/下饭/清淡」标签筛选。
- **搜索**：菜名 + 食材名双维度，基于常驻摘要索引，零延迟。
- **收藏**：详情/结果页星标收藏，IndexedDB 持久化，「记录」页与「我的」页可查。
- **IndexedDB 存储层**（v1 为 localStorage）：favorites / history / prefs 三 store，
  localStorage 自动降级，脏数据 sanitize。
- **Android APK**：免 Gradle 手工链路（aapt2→javac→d8→zipalign→apksigner），
  **零权限**（无 INTERNET）、竖屏锁定、allowBackup=false、完全离线、3.2MB。
- **Capacitor 6 标准工程**：android/ 目录已生成并按要求修改
  （移除 INTERNET 权限、portrait、usesCleartextTraffic=false）。
- **App 图标**：番茄橙圆角底 + 白瓷碗饭 + 平行筷子，480/96/64/40 + 全密度 mipmap。
- **响应式**：rem/clamp 全尺寸、safe-area 四向、100dvh、768px 容器、
  深色模式暖深令牌组、键盘 adjustResize。
- **自检体系**：静态跨文件一致性 6 项 + 真实浏览器端到端 24 项（24/24 通过）。
- **工具链**：gen-manifest（分类文件→摘要索引）、validate-data、压图管线、
  make_icons、build-apk、verify-apk（逐字节核对 + dex 头校验）。

### 设计稿与需求的 4 处统一（用户确认）
1. 步骤页不放步骤图（需求明确不存步骤图），仅步骤序号 + 标题 + 描述。
2. 保留「热量」字段（常见份量估算值），与设计稿详情页一致。
3. 收藏列表入口放在「我的」页 + 「记录」页收藏横滑条。
4. 菜谱库分类条用 6 大分类；「记录」页保留（v1 新增的第 7 屏）。

### 变更
- 单文件形态重构为标准工程（css/js/data 分离），设计令牌与 v1 完全同源。
- 图片从 JPEG 升级为 WebP（长边 800 / 质量 72 / 单张 30~50KB）。

### 修复（开发中踩到）
- data.js 'use strict' 笔误；手撕包菜步骤标题混入英文单词。
- 追加菜谱时吃掉上一道菜的收尾大括号（校验脚本当场拦截）。
- 静态检查 4 个假阳性（tag-- 残根 / ICON 键正则 / data-screen / 拼接前缀路径）。
- aapt2/javac 对中文路径失败 → 构建统一复制到 ASCII 路径执行。
- d8 需要 JAVA_HOME；压缩脚本 q42 仍超 50KB → 质量下限 + 缩边兜底。

### 已知限制
- 85 道菜暂无图（用户确认分批方案），补图前自动降级暖色占位块。
- iOS 仅交付文档（Windows 无法编译 iOS 工程）。

---

## v1.0 — 2026-09-19（更早）

首个版本：7 屏 UI 设计 + 单文件 HTML 落地 + 8 道菜。详见 git 历史。
