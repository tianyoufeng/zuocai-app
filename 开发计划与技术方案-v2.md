# 「今天吃什么」菜谱 App · v2.0 开发计划与技术方案

> 状态：**待用户确认** → 确认后进入编码
> 基线：v1.0（单文件 HTML 版，10 道菜，7 屏 UI，设计令牌齐全）
> 设计依据：`design/今天吃什么-做菜App-UI设计稿.pdf`（7 页已逐页核对）+ `design/设计规范.md`

---

## 一、现状与 v2.0 目标

v1.0 已有：完整 7 屏 UI（首页 / 今日盲盒 / 菜品详情 / 烹饪步骤 / 菜谱库 / 我的口味 / 记录）、
设计令牌体系、加权随机抽菜 + 忌口过滤、份数换算、localStorage 持久化、10 道菜。

v2.0 目标：把 v1 的单文件形态**重构为标准工程**，菜谱库扩容（分阶段到 1000 道），
新增搜索 / 分类浏览 / 收藏（IndexedDB），按 Capacitor 结构组织，免 SDK 手工打包出 Android APK，
iOS 交付完整操作文档。

### 已确认的 4 项决策（2026-09-19 用户拍板）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 规模策略 | **分阶段扩容**：首期 150 道全配真实 AI 成品图，数据结构 / 懒加载 / 图片目录按 1000 道设计，后续迭代补齐 |
| 2 | APK 打包 | **免 Gradle 手工打包链路**（aapt2/javac/d8，本机已验证），同时生成标准 Capacitor android/ 工程目录 |
| 3 | iOS | Windows 无法编译 iOS，交付 **Mac 上 30 分钟可完成的操作文档**（工程生成 + 证书签名 + Xcode 打包） |
| 4 | 设计稿冲突 | ①步骤区不放步骤图；②保留热量字段（估算值）；③收藏列表入口放「我的」页；④分类条=全部+6 大分类；⑤保留「记录」页；仓库继续推 `tianyoufeng/zuocai-app`（v2.0） |

---

## 二、技术架构

### 2.1 工程结构（按需求第七条，落在 zuocai-app 仓库根）

```
zuocai-app/
├── README.md                    # 结构说明 / 进度 / 待办 / 变更日志 / 测试设备清单
├── CHANGELOG.md
├── package.json                 # Capacitor 6.x 依赖声明
├── capacitor.config.json        # appId / appName / orientation: portrait
├── src/
│   ├── index.html
│   ├── css/
│   │   ├── tokens.css           # 设计令牌（对应设计规范.md，含深色模式变量组）
│   │   ├── base.css             # 重置 / 排版 / 安全区 / 响应式骨架
│   │   └── screens.css          # 各屏组件样式
│   ├── js/
│   │   ├── app.js               # 路由 / 屏幕切换（沿用 v1 pushIn/popIn 动画）
│   │   ├── db.js                # IndexedDB 封装（收藏 / 历史 / 偏好 / 忌口）
│   │   ├── data.js              # manifest 加载 / 按分类懒加载 / 搜索索引
│   │   ├── random.js            # 加权随机 + 忌口过滤 + 抽中理由
│   │   └── screens/*.js         # 各屏渲染逻辑
│   ├── data/
│   │   ├── manifest.js          # 全量摘要索引（id/菜名/分类/标签/时间/难度/热量）
│   │   └── {分类}.js             # 每分类一个文件：window.RECIPES_xx = [...]
│   └── assets/
│       ├── icons/               # App 图标 480/96/64/40
│       └── images/{分类}/       # 菜谱图 {id}.webp，按分类存放
├── android/                     # Capacitor 结构生成的 Android 工程（含移除 INTERNET 权限的 Manifest）
├── ios/                         # 占位 + iOS 打包文档入口（工程需在 Mac 上生成）
├── docs/
│   ├── ios-build-guide.md       # iOS 工程生成 / 证书 / 签名 / Xcode 打包全流程
│   └── apk-build-guide.md       # Android APK 打包命令（可复制即用，含环境安装步骤）
└── tools/                       # 图片压缩脚本 / 数据校验脚本 / 打包脚本
```

### 2.2 关键技术决策

**① 数据文件用 `.js` 而非 `.json`（双环境兼容的关键）**
`fetch('./data/家常菜.json')` 在浏览器 `file://` 协议下被 CORS 拦死，双击 index.html 就白屏；
而 APK 内 Capacitor 默认走 `https://localhost` 没问题，但「浏览器直接预览」是需求的交付项之一。
因此每个分类数据写成 `window.RECIPES_jiachangcai=[...]` 的 JS 文件，用动态 `<script>` 注入按需加载
—— file://、http://、APK 三种环境统一兼容，零 CORS 问题。

**② 搜索用「摘要 manifest」常驻 + 详情按分类懒加载**
manifest 含每道菜的 id / 菜名 / 分类 / 标签 / 时间 / 难度 / 热量 / 食材名列表（用于食材搜索），
1000 道约 150-250KB，启动一次性加载无压力；完整步骤数据只在进入详情时加载对应分类文件并缓存。

**③ IndexedDB 替代 v1 的 localStorage（需求明确）**
对象仓库：`favorites`（收藏）、`history`（抽菜/做过记录）、`prefs`（口味偏好/忌口/份数）。
封装 `db.js` 提供 get/set/del/all，含版本迁移与脏数据 sanitize（沿用 v1 经验）。

**④ 完全离线 + 零网络权限**
- 手工打包的 AndroidManifest **不声明任何权限**（天然满足「移除 INTERNET」）；
- WebView 加载 `file:///android_asset/www/index.html`，所有资源本地相对路径；
- 风险预案：若个别机型 WebView 对 asset 加载异常，回退为 `WebViewAssetLoader` 拦截方案
  （同样不需要 INTERNET 权限），真机测试列入验收清单。

**⑤ 响应式（需求第六条逐条落实）**
- 375px 基准，`html { font-size: clamp(14px, 4.267vw, 17px) }`，尺寸全用 rem/% /clamp，无写死 px 宽度；
- viewport `width=device-width, initial-scale=1, viewport-fit=cover` + 四向 safe-area 变量；
- 底部导航 `padding-bottom: calc(12px + env(safe-area-inset-bottom))`；
- 视口高度用 `100dvh`，并留 `--vh` JS 兜底（老 WebView 不支持 dvh 时）；
- 容器 `max-width: 768px; margin: 0 auto`（平板 / 折叠屏）；
- 深色模式：`prefers-color-scheme: dark` 切换暖深色令牌组（底 #1C1A17 / 卡片 #262320 / 文字反白），
  系统字体放大走 rem 自动跟随；
- Capacitor Keyboard `resize: body`，搜索框 `scrollIntoView` 兜底（手工壳内对应设置
  `android:windowSoftInputMode="adjustResize"`）。

### 2.3 菜谱数据规格（需求第四条）

| 项 | 规格 |
|---|---|
| 首期数量 | 150 道（家常菜 60 / 汤羹 20 / 主食 25 / 凉菜 15 / 早餐 15 / 夜宵 15） |
| 字段 | id、name、category、image、time、difficulty、calories（估算）、tags[]、ingredients[{name, amount}]、steps[{no, text}]、tips（可选） |
| 图片 | WebP、最长边 800px、质量 70~75、单张 30~50KB；列表/详情复用同一张；`object-fit: cover` 裁缩略图 |
| 存放 | `src/assets/images/{分类}/{id}.webp` |
| 文本 | 每道菜真实食材用量 + 分步文字做法（自写，非拼凑模板）；按分类拆文件懒加载 |
| 校验 | `tools/validate-data.js`：字段完整性、id 唯一、图片文件存在、重复菜名检测 |

### 2.4 图片管线（延续 v1 已验证链路）

ImageGen 生成 1536×1024 实拍风成品图 → Pillow 压缩：长边 800 / WebP q72 → 入库。
首期 150 张 ≈ 6MB；1000 道全量后 ≈ 40MB，加 APK 壳约 45-50MB。
**体积说明（主动申报）**：按 40KB/张推算，即使扩到 1500 道安装包也只有 ~65MB，
达不到需求写的 80~120MB「目标区间」——该区间下限不是约束目的，**上限 150MB 不会被触碰**，
且远低于它意味着安装更快，属正向偏离，不额外注水。

### 2.5 App 图标（需求第五条）

SVG 绘制（碗 + 筷子简化图形，番茄橙 #E8703A 圆角底）→ PNG 导出 480/96/64/40 四档，
入 `src/assets/icons/`，同时映射到 Android 工程的 `res/mipmap-*`。

---

## 三、开发阶段拆分

| 阶段 | 内容 | 产出 |
|---|---|---|
| P0 | 工程骨架：目录、tokens.css、base.css、7 屏路由壳、package.json、capacitor.config.json | 浏览器可打开的空壳 |
| P1 | 数据管线：schema、manifest、150 道菜谱文本分批写入（家常菜先行）、校验脚本 | 数据全就位 |
| P2 | 图片管线：150 张成品图生成 + 压缩 + 入库 + 引用核对 | 图文一一对应 |
| P3 | 功能开发：随机抽菜（加权+忌口+理由）、菜谱库（分类条+双列列表）、详情（食材/步骤/收藏）、搜索（菜名+食材）、记录页、我的（口味/忌口/设置/收藏列表） | 功能完整 |
| P4 | 适配：safe-area / 100dvh / 深色模式 / 字体放大 / 320~768px 无横向滚动 / 键盘 | 适配达标 |
| P5 | 图标 + Android 工程（Manifest 零权限）+ 手工链路打 APK + 真机安装自测 | 可安装 APK |
| P6 | 测试矩阵（iPhone SE/15、Android 360×640 / 412×915、iPad 模拟）+ README/CHANGELOG | 文档齐 |
| P7 | git 提交（feat:/fix: 规范）+ 推送 GitHub + 归档 `D:\Before_file\WorkBuddy_File\` 带版本目录 + iOS 打包文档 | 全量交付 |

每阶段完成即在 README.md 记变更，commit 按阶段粒度提交。

---

## 四、风险与对策

| 风险 | 对策 |
|---|---|
| 1000 道图的产量瓶颈 | 已确认分阶段；每轮迭代按分类批量补 30-80 道，manifest 设计一次到位，补菜不动架构 |
| 移除 INTERNET 后个别机型 WebView 异常 | asset 直载 → WebViewAssetLoader 两级预案，列入真机验收 |
| 生图风格漂移（150 张不一致） | 固定同一提示词模板（暖木桌面 / 白瓷盘 / 自然光，与 v1 十图同风格系） |
| 中文路径 / 文件名 | 代码、图片文件名全用 ASCII（分类目录用拼音），仅菜名等显示文案用中文 |
| IndexedDB 老WebView兼容 | Android 8.0+ / iOS 14 原生支持，无需 polyfill；首次打开失败时降级 localStorage 并提示 |

---

## 五、交付物清单

1. `src/` 完整前端工程（浏览器可直接预览，双击 index.html 可运行）
2. 可安装的 Android APK（零网络权限、完全离线、竖屏锁定）+ 可复制即用的打包命令文档
3. Capacitor 6.x 标准工程结构（android/ 目录 + 配置）+ iOS Mac 端操作文档
4. README（结构/进度/待办/变更/测试设备清单）、CHANGELOG、图标四档
5. GitHub 仓库 `tianyoufeng/zuocai-app` 全量推送 + 本地带版本号归档

---

## 六、待你确认

本计划已含你确认过的 4 项决策，若无补充修改，回复「确认」即从 P0 开始编码。
若对菜谱分类配比（150 道的分布）、首批标签体系（快手/下饭/清淡/低脂…）想调整，现在提出最省事。
